% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain-trait.R
\name{explain_traits}
\alias{explain_traits}
\title{Explain Multiple Derived Traits}
\usage{
explain_traits(
  trait_fns,
  use_ai = FALSE,
  custom_mp = NULL,
  provider = getOption("glydet.ai_provider", "deepseek"),
  model = getOption("glydet.ai_model", NULL),
  api_key = getOption("glydet.ai_api_key", NULL),
  base_url = getOption("glydet.ai_base_url", NULL)
)
}
\arguments{
\item{trait_fns}{A list of derived trait functions created by the trait
factories.}

\item{use_ai}{Whether to use a Large Language Model (LLM) to explain the
traits. Default is \code{FALSE}.}

\item{custom_mp}{A named character vector of custom meta-properties. Only
used when \code{use_ai = TRUE}.}

\item{provider}{AI provider passed to \code{ellmer} when \code{use_ai = TRUE}.}

\item{model}{Model to use when \code{use_ai = TRUE}.}

\item{api_key}{API key for the selected provider when \code{use_ai = TRUE}.}

\item{base_url}{Optional base URL for custom or OpenAI-compatible endpoints.}
}
\value{
A character vector of trait explanations. Input names are preserved;
entries that cannot be explained are \code{NA}.
}
\description{
Explain a list of derived trait functions. With \code{use_ai = TRUE}, all valid
traits are sent in one request so the shared prompt is only sent once.
Invalid traits, or traits that the AI cannot explain, are returned as \code{NA}
and generate a warning without preventing the remaining traits from being
explained.
}
\examples{
explain_traits(traits_basic()[c("TM", "GS")])

}
